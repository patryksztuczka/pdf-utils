# PDF Organizer as the Single Workflow

PDF Util will expose the PDF Organizer as the single primary workflow and remove Merge PDF and Split PDF as standalone tools. Users primarily need to assemble pages from one or more source PDFs, rotate and group pages, and export the resulting workspace; merge and split outcomes should exist inside that organizer workflow rather than competing with it as separate routes and screens.
